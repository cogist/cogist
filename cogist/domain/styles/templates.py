"""Preset templates for mind map styling.

This module is reserved for future template factory functions.
Currently, all templates are loaded from JSON files (user directory or built-in assets).

For testing purposes, use the _create_test_template() helper in test files.
"""

# This module is intentionally minimal.
# Template creation logic has been moved to:
# - Production: JSON file loading (template_loader.py)
# - Testing: _create_test_template() in test files
